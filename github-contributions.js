// Generated by CoffeeScript 1.6.3
var api, fetch, github, notify, process;

api = require('github');

github = new api({
  version: '3.0.0'
});

fetch = function(page, total) {
  return github.repos.getFromUser({
    page: page,
    per_page: 100,
    user: 'eighttrackmind'
  }, function(err, res) {
    return process(res, total, page);
  });
};

process = function(res, total, page) {
  var count;
  count = res.length;
  if (count) {
    total += count;
    return fetch(++page, total);
  } else {
    return notify(total);
  }
};

notify = function(total) {
  return console.log(total);
};

fetch(1, 0);
